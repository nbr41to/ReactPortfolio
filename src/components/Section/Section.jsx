import React from "react"
import { StyledComponent } from "./Section.styled"

export default Section = ({ title, text }) => {
    <StyledComponent className="main item">
        <section>
            <h2>{title}</h2>
            <p>{text}</p>
        </section>
    </StyledComponent>
}